import urllib.request, json; req = urllib.request.Request('https://api.github.com/repos/felix16805/social-media-content-analyzer/actions/runs', headers={'Accept': 'application/vnd.github+json'}); res = urllib.request.urlopen(req); runs = json.loads(res.read()).get('workflow_runs', []); [print(str(r['id']) + ':' + r['name'] + ':' + str(r['conclusion'])) for r in runs[:5]]
